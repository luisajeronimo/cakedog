<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="SwitchStyle" parent="@style/Widget.AppCompat.CompoundButton.Switch">
        <item name="android:scaleX">1</item> <!-- Ajuste o fator de escala horizontal -->
        <item name="android:scaleY">1</item> <!-- Ajuste o fator de escala vertical -->
        <item name="android:thumbTint">@color/padrao</item>
    </style>
</resources>